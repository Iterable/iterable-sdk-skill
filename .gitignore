.DS_Store
scaffold.sh
*.local
node_modules/
pipeline/dist/
proposals/

# Eval derived artifacts — regenerated from scenarios/ + transcripts/.
# Committed: scenarios, *.sample.md transcripts, eval/README.md.
eval/prompts/
eval/results/
eval/report/

# JetBrains IDE files
.idea/

# Agent tooling
.agent/
.artifacts/
.claude/
.cursor/
.opencode/
.pi/

# The tool writes its workspace into the project being integrated. Present here
# only when someone runs it against this repo; it holds a service-account key.
.iterable/
